[
  {
    "algorithm": "LogisticRegression",
    "family": "classification",
    "description": "Logistic regression for binary PD classification.",
    "params": [
      { "name": "C",          "type": "float",  "default": 1.0,    "description": "Inverse of regularisation strength" },
      { "name": "max_iter",   "type": "int",    "default": 200,    "description": "Maximum iterations" },
      { "name": "solver",     "type": "string", "default": "lbfgs","description": "Optimisation algorithm" }
    ]
  },
  {
    "algorithm": "RandomForest",
    "family": "classification",
    "description": "Ensemble of decision trees for classification.",
    "params": [
      { "name": "n_estimators",    "type": "int",   "default": 100,   "description": "Number of trees" },
      { "name": "max_depth",       "type": "int",   "default": null,  "description": "Max tree depth (null = unlimited)" },
      { "name": "min_samples_leaf","type": "int",   "default": 1,     "description": "Min samples per leaf" }
    ]
  },
  {
    "algorithm": "GradientBoosting",
    "family": "classification",
    "description": "Gradient boosted trees (sklearn).",
    "params": [
      { "name": "n_estimators",  "type": "int",   "default": 100,  "description": "Boosting rounds" },
      { "name": "learning_rate", "type": "float", "default": 0.1,  "description": "Step size shrinkage" },
      { "name": "max_depth",     "type": "int",   "default": 3,    "description": "Max tree depth" }
    ]
  },
  {
    "algorithm": "LightGBM",
    "family": "classification",
    "description": "Light Gradient Boosting Machine.",
    "params": [
      { "name": "num_leaves",    "type": "int",   "default": 31,   "description": "Max leaves per tree" },
      { "name": "learning_rate", "type": "float", "default": 0.05, "description": "Step size shrinkage" },
      { "name": "n_estimators",  "type": "int",   "default": 200,  "description": "Boosting rounds" }
    ]
  },
  {
    "algorithm": "ARIMA",
    "family": "timeseries",
    "description": "AutoRegressive Integrated Moving Average.",
    "params": [
      { "name": "p", "type": "int", "default": 1, "description": "AR order" },
      { "name": "d", "type": "int", "default": 1, "description": "Differencing order" },
      { "name": "q", "type": "int", "default": 1, "description": "MA order" }
    ]
  },
  {
    "algorithm": "OLS",
    "family": "timeseries",
    "description": "Ordinary Least Squares regression.",
    "params": [
      { "name": "fit_intercept", "type": "bool", "default": true, "description": "Whether to fit intercept" }
    ]
  },
  {
    "algorithm": "Ridge",
    "family": "timeseries",
    "description": "Ridge regression with L2 regularisation.",
    "params": [
      { "name": "alpha", "type": "float", "default": 1.0, "description": "Regularisation strength" }
    ]
  },
  {
    "algorithm": "GLM_Binomial",
    "family": "statistical",
    "description": "Generalised Linear Model with Binomial family (logit link).",
    "params": [
      { "name": "fit_intercept", "type": "bool", "default": true, "description": "Include intercept" }
    ]
  },
  {
    "algorithm": "GLM_Gaussian",
    "family": "statistical",
    "description": "Generalised Linear Model with Gaussian family (identity link).",
    "params": [
      { "name": "fit_intercept", "type": "bool", "default": true, "description": "Include intercept" }
    ]
  },
  {
    "algorithm": "CoxPH",
    "family": "statistical",
    "description": "Cox Proportional Hazards model for survival analysis.",
    "params": [
      { "name": "ties", "type": "string", "default": "breslow", "description": "Method for handling tied event times" }
    ]
  }
]
