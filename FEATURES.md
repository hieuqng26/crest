Datasets page
- "All", "Upload", "Live Query" filters should be grouped into a dropdown (default to "All")
- Redesign individual dataset's page when click "View": some components are not well aligned, the data's text size is too small
- Add ability to bulk delete

Algorithm Catalog
- Rename to Model Catalog

Saved Configurations
- deleted configuration still exists
- add ability to edit each configuration
- Bulk delete saved configurations
- Remove New Configuration button in Saved Configurations page

New Calibration Run page:
- Each dataset box under Datasets section : there should be ability to view all columns
- Remove Resulting Schema section

Individual Calibration's page:
- show model's fitted coefficients (if applicable), best hyperparameters (if using grid search or random search)

All Datetime should show format DD/MM/YYYY HH:MM:SS in timezone (add this as system variable)

Format all python scripts according industry standard python formatting

Add regression models